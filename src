import "./globals.css";

export const metadata = {
  title: "MoreTalent",
  description: "Recruitment automation for DG Ventures",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
